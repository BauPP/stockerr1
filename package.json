{
  "scripts": {
    "setup:deps": "node scripts/setup-deps.js",
"verify:all": "npm --prefix services/auth-service test && npm --prefix services/category-service test && npm --prefix api-gateway test",
    "verify:ms02": "npm --prefix services/user-service test"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.20.0"
  }
}
