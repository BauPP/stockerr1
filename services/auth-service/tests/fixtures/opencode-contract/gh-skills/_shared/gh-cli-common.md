## Shared GH CLI Contract

- Ejecuta `gh auth status` antes de acciones sensibles.
- No ejecutes acciones sensibles sin verificación previa.
- No uses flags para saltar controles.
- El contrato cubre fusionar, aprobar, publicar y liberar con ownership explícito.
- Commit policy: `type(scope): descripción en español`.
